[bria]

# webhook source (with HMAC) -> pg sink + pg state
[global]
tmp_dir = "/tmp/bria"

[bria.global.log]
level = "info"
format = "json"

[bria.global.state]
backend = "pg"
pg_url = "${BRIA_E2E_PG_URL}"

[bria.server]
enabled = true
bind = "0.0.0.0"
port = 4000
prefix = "v1"
api_key = "${BRIA_API_KEY}"

[[bria.sources]]
id = "hooks"
type = "webhook"
path = "hooks"
hmac_secret = "${BRIA_E2E_WEBHOOK_SECRET}"
hmac_header = "X-Bria-Signature"
id_field = "id"
max_body_bytes = 1048576

[[bria.tasks]]
id = "echo"
driver = "local"
cmd = "sh"
args = ["-c", "printf '{\"input\":\"%s\",\"pipeline\":\"%s\"}' \"$1\" \"$PIPELINE_ID\"", "sh", "{{job.payload.message}}"]
timeout_secs = 10

[bria.tasks.stdout]
mode = "capture"
max_bytes = 4096

[bria.tasks.stderr]
mode = "capture"
max_bytes = 4096

[[bria.sinks]]
id = "pg-out"
type = "pg"
url = "${BRIA_E2E_PG_URL}"

[bria.sinks.table]
name = "bria_results"

[[bria.pipelines]]
id = "webhook-pg-pipeline"
source = "hooks"
sinks = ["pg-out"]

[[bria.pipelines.steps]]
id = "run"
type = "process"
task = "echo"
