# Long-lived infra services shared across all e2e scenarios.
# Brought up once at the start of the test suite and torn down at the end.
services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: bria
      POSTGRES_USER: bria
      POSTGRES_PASSWORD: bria
    ports:
      - "54328:5432"
    configs:
      - source: postgres_init_sql
        target: /docker-entrypoint-initdb.d/01-init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bria -d bria"]
      interval: 2s
      timeout: 5s
      retries: 30

  rabbitmq:
    image: rabbitmq:4-alpine
    environment:
      RABBITMQ_DEFAULT_USER: bria
      RABBITMQ_DEFAULT_PASS: bria
      RABBITMQ_ERLANG_COOKIE: bria-e2e-cookie
    ports:
      - "5672:5672"
    configs:
      - source: rabbitmq_conf
        target: /etc/rabbitmq/conf.d/99-e2e.conf
    healthcheck:
      test: ["CMD-SHELL", "rabbitmq-diagnostics -q ping"]
      interval: 2s
      timeout: 5s
      retries: 30
      start_period: 10s

  webhook-echo:
    image: python:3-alpine
    command: ["python3", "-u", "/scripts/webhook-echo.py", "8080"]
    configs:
      - source: webhook_echo_py
        target: /scripts/webhook-echo.py
        mode: 0755
    ports:
      - "8080:8080"

  amqp-helper:
    build:
      context: .
      dockerfile: amqp-helper.Dockerfile
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      AMQP_URL: ${BRIA_E2E_AMQP_HELPER_URL:-amqp://bria:bria@rabbitmq:5672/%2F}
    healthcheck:
      test: ["CMD-SHELL", "cat /tmp/ready"]
      interval: 2s
      timeout: 5s
      retries: 30

networks:
  default:
    name: e2e-net

configs:
  rabbitmq_conf:
    content: |
      deprecated_features.permit.transient_nonexcl_queues = true

  postgres_init_sql:
    content: |
      CREATE TABLE IF NOT EXISTS bria_results (
          result_id   TEXT PRIMARY KEY,
          job_id      TEXT NOT NULL,
          pipeline_id TEXT NOT NULL,
          step_id     TEXT NOT NULL,
          occurred_at TEXT NOT NULL,
          exit_code   INTEGER NOT NULL,
          stdout      TEXT,
          stderr      TEXT,
          duration_ms BIGINT NOT NULL,
          attempt     INTEGER NOT NULL,
          status      TEXT NOT NULL
      );

      CREATE TABLE IF NOT EXISTS bria_jobs (
          id          TEXT PRIMARY KEY,
          payload     TEXT NOT NULL DEFAULT '{}',
          status      TEXT,
          created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
      );

  webhook_echo_py:
    content: |
      import json
      import sys
      from http.server import HTTPServer, BaseHTTPRequestHandler

      class Handler(BaseHTTPRequestHandler):
          def do_POST(self):
              length = int(self.headers.get("content-length", "0"))
              body = self.rfile.read(length) if length > 0 else b""
              entry = {
                  "path": self.path,
                  "method": "POST",
                  "headers": dict(self.headers),
                  "body": body.decode("utf-8", errors="replace"),
              }
              self.send_response(200)
              self.end_headers()
              self.wfile.write(b'{"status":"ok"}\n')
              print(json.dumps(entry), flush=True)

          def do_GET(self):
              self.send_response(200)
              self.end_headers()
              self.wfile.write(b'{"requests":[]}\n')

      if __name__ == "__main__":
          port = int(sys.argv[1]) if len(sys.argv) > 1 else 8080
          print(f"webhook-echo listening on :{port}", flush=True)
          HTTPServer(("0.0.0.0", port), Handler).serve_forever()


